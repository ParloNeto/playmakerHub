import { FifaCareerDirective } from './fifa-career.directive';

describe('FifaCareerDirective', () => {
  it('should create an instance', () => {
    const directive = new FifaCareerDirective();
    expect(directive).toBeTruthy();
  });
});
